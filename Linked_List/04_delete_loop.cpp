#include<bits/stdc++.h>
using namespace std;

struct Node
{
    int data;
    struct Node *next;
};


class Solution {
  public:

    // using slow and fast pointers  
    bool deleteLoop(Node* head) {
        
    }
};